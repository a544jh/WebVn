(Keep YAML lib up to date (lots of API changes in current 2.0.0 version...))

Rollback & History

Save/Load

Better YAML parsing, actors etc..

Standalone Player entrypoint

---

NVL Mode

Note (text message) system

---

Sprite flipping

Position presets

Arbitrary trasformation matrix for bg

Rewrite audio renderer with WebAudiContext (to solve chrome looping gap...)